[project]
name = "assistente-pessoal"
version = "0.1.0"
description = "Assistente pessoal inteligente executado em um Raspberry Pi."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]